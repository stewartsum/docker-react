language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t stewartsum/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true stewartsum/docker-react npm run test
